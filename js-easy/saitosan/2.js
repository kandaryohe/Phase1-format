const setBtn = function () {
  const hoge = Math.random() * 10;
  if (hoge < 5) {
    alert("ぺっぺぺー");
  } else {
    alert("斎藤さんだぞ");
  }
};

//ボタンを押した時関数が呼び出されるようにする
//作成した関数にランダムで0～10までの数字を出力すようにする
